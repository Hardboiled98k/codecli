# CodeCLI 社区版隐私说明

**版本**：2026-07-16
**维护者**：南京孤岛网络科技有限公司
**隐私联系**：825242058@qq.com

本说明适用于本仓库中的 CodeCLI 开源社区版。第三方 fork 或重新分发版本可能改变数据处理方式，应由其发布者另行说明。

## 1. 核心承诺

社区版：

- 不包含使用分析、广告追踪、设备画像或崩溃自动上报 SDK；
- 不包含远程 Shell、SSH 接入、Tailscale/Headscale 会话、操作员接管或远程控制服务；
- 不连接 CodeCLI 维护者运营的激活、兑换码或设备绑定服务；
- 不把 Provider API Key 上传给 CodeCLI 维护者。

“无遥测”不等于“完全离线”。安装工具、打开下载页、检查第三方版本或验证 Provider 时，应用需要访问对应的第三方网络资源。

## 2. 本机处理的数据

为完成用户选择的检查、安装、配置、备份、恢复和卸载，CodeCLI 可能在本机处理：

- 操作系统、架构、磁盘、网络和已安装工具的状态；
- Node、Claude Code、Codex CLI 等工具的路径和版本；
- 用户选择的 Provider、Base URL、模型名和配置方案；
- 用户主动输入的 Provider API Key；
- CodeCLI 创建的配置 ownership 记录、备份、事务状态和脱敏诊断日志。

这些内容保存在用户设备上。某些第三方 CLI 为正常工作，可能要求把 Key 写入其约定配置文件或用户级环境变量；界面执行前应说明目标位置。

## 3. 网络请求

根据用户选择，CodeCLI 可能：

- 在环境自检时访问 `registry.npmjs.org` 和 `nodejs.org`，判断实际安装所需的官方端点是否可达；
- 从 Node.js、npm registry 或其他明确标注的官方来源下载受支持工具；
- 打开 Anthropic、OpenAI 或其他第三方的官方下载/文档页面；
- 向用户选择的 Provider 发送最小连通性测试请求；
- 调用用户安装的第三方 CLI，而该 CLI 可能根据其自身功能访问网络。

网络连接会向相应第三方暴露通常的连接数据，例如来源 IP、请求时间和 User-Agent。Provider 连通性测试会把用户提供的 API Key 直接发送给该 Provider。上述第三方的数据处理受其各自隐私政策与条款约束。

## 4. 日志与密钥

CodeCLI 设计为对常见 Authorization、Token、Key 和 URL 查询参数进行脱敏，但自动脱敏无法保证覆盖所有自定义格式。公开 Issue 或发送日志前，请再次人工检查。

请不要通过 Issue、邮件、截图或聊天发送完整 API Key、密码、Token、Cookie、私钥、个人项目文件或其他无关信息。维护者不会索要这些内容，也不会要求远程控制你的设备。

## 5. 删除本地数据

用户可以使用应用提供的恢复、清理或卸载功能删除 CodeCLI 管理的方案、备份、日志和本地状态。为避免数据损失，CodeCLI 不应删除无法证明由其创建或管理的文件。用户自行导出的诊断文件需要由用户自行删除。

卸载 CodeCLI 不会自动删除第三方 CLI 自己创建的数据或用户在第三方服务中的账号数据。

## 6. 主动联系维护者

当用户通过 GitHub Issue、Pull Request 或邮件联系维护者时，GitHub 或邮件服务商会依其自身政策处理账号、邮件地址、IP 和消息内容。维护者仅使用收到的信息处理对应问题，并尽量避免复制不必要的个人信息。

如需查询、更正或删除你主动提交给维护者的信息，请联系 **825242058@qq.com**。为保护他人信息，处理请求时可能需要进行最小必要的身份核对。

## 7. 变更

如果社区版未来增加新的网络服务或数据处理能力，必须在合并前更新本说明，并在界面中给予清楚提示。远程控制或静默遥测不属于当前社区版边界。
